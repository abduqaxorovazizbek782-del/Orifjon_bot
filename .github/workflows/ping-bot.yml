name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Har 5 daqiqada
  workflow_dispatch:  # Qo'lda ishga tushirish uchun

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Bot
        run: curl https://orifjon-bot.onrender.com/
